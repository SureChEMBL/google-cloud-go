From: Shengjing Zhu <i@zhsj.me>
Date: Wed, 13 Sep 2017 13:35:36 +0800
Subject: skip test needing network

Forwarded: not-needed
Signed-off-by: Shengjing Zhu <i@zhsj.me>
---
 compute/metadata/metadata_test.go  | 1 +
 logging/logging_test.go            | 4 ++++
 logging/logging_unexported_test.go | 1 +
 3 files changed, 6 insertions(+)

diff --git a/compute/metadata/metadata_test.go b/compute/metadata/metadata_test.go
index 9ac5926..5e55125 100644
--- a/compute/metadata/metadata_test.go
+++ b/compute/metadata/metadata_test.go
@@ -38,6 +38,7 @@ func TestOnGCE_Stress(t *testing.T) {
 }
 
 func TestOnGCE_Force(t *testing.T) {
+	t.Skip("skipping test needing network")
 	onGCEOnce = sync.Once{}
 	old := os.Getenv(metadataHostEnv)
 	defer os.Setenv(metadataHostEnv, old)
diff --git a/logging/logging_test.go b/logging/logging_test.go
index 3bc87e9..bf72e3b 100644
--- a/logging/logging_test.go
+++ b/logging/logging_test.go
@@ -151,6 +151,7 @@ func initLogs(ctx context.Context) {
 // Testing of Logger.Log is done in logadmin_test.go, TestEntries.
 
 func TestLogSync(t *testing.T) {
+	t.Skip("skipping test needing network")
 	initLogs(ctx) // Generate new testLogID
 	ctx := context.Background()
 	lg := client.Logger(testLogID)
@@ -191,6 +192,7 @@ func TestLogSync(t *testing.T) {
 }
 
 func TestLogAndEntries(t *testing.T) {
+	t.Skip("skipping test needing network")
 	initLogs(ctx) // Generate new testLogID
 	ctx := context.Background()
 	payloads := []string{"p1", "p2", "p3", "p4", "p5"}
@@ -315,6 +317,7 @@ func cleanNext(it *logadmin.EntryIterator) (*logging.Entry, error) {
 }
 
 func TestStandardLogger(t *testing.T) {
+	t.Skip("skipping test needing network")
 	initLogs(ctx) // Generate new testLogID
 	ctx := context.Background()
 	lg := client.Logger(testLogID)
@@ -382,6 +385,7 @@ func TestParseSeverity(t *testing.T) {
 }
 
 func TestErrors(t *testing.T) {
+	t.Skip("skipping test needing network")
 	initLogs(ctx) // Generate new testLogID
 	// Drain errors already seen.
 loop:
diff --git a/logging/logging_unexported_test.go b/logging/logging_unexported_test.go
index cd4b6eb..168962a 100644
--- a/logging/logging_unexported_test.go
+++ b/logging/logging_unexported_test.go
@@ -32,6 +32,7 @@ import (
 )
 
 func TestLoggerCreation(t *testing.T) {
+	t.Skip("skipping test needing network")
 	const logID = "testing"
 	c := &Client{projectID: "PROJECT_ID"}
 	customResource := &mrpb.MonitoredResource{
